#ifndef SORT_H
#define SORT_H
#include "types.h"  // Must be in same directory

// Parameters: Reservation struct array and its length
void sortrByDate(Reservation* reservations, int num);
Reservation* rsortrByDate(Reservation* reservations, int num);

#endif
